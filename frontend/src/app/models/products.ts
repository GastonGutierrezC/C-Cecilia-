export interface ProductModel {
  id: number;
  name: string;
  inPrice: number;
  sellPrice: number;
  image: string;
}

export interface ProductData {
  name: string;
  inPrice: number;
  sellPrice: number;
  image: string;
}
